<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vibeapp.post.PostRepository">

    <select id="findAll" resultType="com.example.vibeapp.post.Post">
        SELECT * FROM POSTS ORDER BY id DESC
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM POSTS
    </select>

    <select id="findPage" resultType="com.example.vibeapp.post.Post">
        SELECT * FROM POSTS 
        ORDER BY id DESC 
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" resultType="com.example.vibeapp.post.Post">
        SELECT * FROM POSTS WHERE id = #{id}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO POSTS (title, content, created_at, views)
        VALUES (#{title}, #{content}, #{createdAt}, #{views})
    </insert>

    <update id="update">
        UPDATE POSTS
        SET title = #{title},
            content = #{content},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM POSTS WHERE id = #{id}
    </delete>

    <update id="incrementViews">
        UPDATE POSTS SET views = views + 1 WHERE id = #{id}
    </update>

</mapper>
